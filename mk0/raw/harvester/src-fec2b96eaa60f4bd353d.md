# Project Instructions (ARC.md)

These instructions are automatically prepended to the system prompt of every agent.

## Core Constraints
- Always break down tasks before implementing them.
- This is a Rust project. Prioritize `std` when possible, use explicit error propagation (`?`), and document all public traits.
- Format code with `cargo fmt`.
