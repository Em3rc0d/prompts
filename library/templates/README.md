# Templates

Parameterized prompt shells whose reusable value comes from explicit variable slots.

Template files should document required variables, optional variables, intended models, source provenance and at least one safe usage example when available.
